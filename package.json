{
  "name": "roboledger",
  "version": "0.1.0",
  "private": true,
  "description": "Decentralized robot task marketplace on Hedera",
  "scripts": {
    "build": "pnpm -r build",
    "setup": "tsx scripts/setup-testnet.ts",
    "demo": "tsx scripts/demo.ts"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.8.0"
  }
}
